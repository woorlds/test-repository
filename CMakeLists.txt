# CMake build script for the test-repository project

# Linked list test project
PROJECT(linked-list)

# CMake minimum requirement version
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

# Executable
ADD_EXECUTABLE(linked-list linked-list.c)
